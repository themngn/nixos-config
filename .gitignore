# Nix build results
result
result-*

# Hardware-specific config — copy manually to each host, do not commit
# Uncomment the line below if you want to track it in git (single-machine setups)
# hosts/*/hardware-configuration.nix

# Nix garbage
.direnv/
.devenv/

# Editor junk
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
